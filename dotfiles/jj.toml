[user]
name = "Kyle J Strand"
email = "kstrand@rigetti.com"

[ui]
# diff.tool = ["difft", "--color=always", "$left", "$right"]
diff-editor = "vscode"
merge-editor = "vscode"
paginate = "never"
default-command = "status"

[revsets]
log = "trunk()..@ | trunk()"

[experimental-advance-branches]
enabled-branches = ["glob:*"]
disabled-branches = ["main", "master", "glob:rigetti-integration-*", "glob:integration-rigetti-*"]

# [git]
# abandon-unreachable-commits = false
