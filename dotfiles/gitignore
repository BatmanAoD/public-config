*.swp
*.swo
lib*.rlib
.idea
*.old
*.pyc
*.ignore
*.bak_gr
neovide_backtraces.log
.vscode/*
.DS_Store
certs

# Remove this if we ever decide to start checking pyenv files into source repos
.python-version

# Go debugging
__debug_bin*

# Never ignore .github or .gitlab files
!/.github/
!/.gitlab*
# ...this one is primarily for RipGrep use across repo boundaries:
# don't ignore gitlab-CI files, even when they're not at the root of the current repo.
!.gitlab-ci.yml

.tmp/
